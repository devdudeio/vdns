vrsc:{$VNS_DNS_PORT} {
  bind 127.0.0.1

  vns {
    resolver_url {$VNS_RESOLVER_URL}
    timeout 3s
  }

  errors
  log
  cache 30
}

.:{$VNS_DNS_PORT} {
  bind 127.0.0.1

  forward . {$VNS_UPSTREAM_DNS_1} {$VNS_UPSTREAM_DNS_2}
  cache 300
  errors
  log
}
